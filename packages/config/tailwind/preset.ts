import type { Config } from "tailwindcss";

/**
 * Единый источник дизайн-токенов для всех приложений монорепозитория
 * (web - клуб, erp - админ-панель). Любые цвета, шрифты и радиусы задаются
 * здесь, чтобы интерфейсы оставались консистентными и без хардкода.
 *
 * Палитра тёплая и пастельная: бежевый нейтральный тон (sand) и мягкий
 * терракотовый акцент (clay).
 */
const preset = {
  content: [],
  theme: {
    extend: {
      colors: {
        // Нейтральный тёплый бежевый - фоны, границы, текст.
        sand: {
          50: "#faf7f1",
          100: "#f3ebdd",
          200: "#e8d9c2",
          300: "#d8c0a0",
          400: "#c5a47d",
          500: "#b58e63",
          600: "#a07a52",
          700: "#846246",
          800: "#6c513d",
          900: "#594435",
          950: "#2f241b",
        },
        // Мягкий терракотовый акцент - кнопки, ссылки, активные состояния.
        clay: {
          50: "#faf4f0",
          100: "#f2e4da",
          200: "#e5c9b6",
          300: "#d4a78c",
          400: "#c28468",
          500: "#b56a4d",
          600: "#a0573f",
          700: "#854636",
          800: "#6e3b30",
          900: "#5c342b",
          950: "#301914",
        },
      },
      fontFamily: {
        sans: ["var(--font-sans)", "system-ui", "sans-serif"],
        serif: ["var(--font-serif)", "Georgia", "serif"],
      },
      borderRadius: {
        xl: "0.875rem",
        "2xl": "1.125rem",
        "3xl": "1.5rem",
      },
    },
  },
  plugins: [],
} satisfies Partial<Config>;

export default preset;
